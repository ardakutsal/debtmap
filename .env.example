REDIS_URL=redis://localhost:6379/0
DATABASE_URL=sqlite:///./debtmap.db
# REQUIRED. Generate with:
#   python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
FERNET_KEY=<generate-with-the-command-above>
# Optional: used for higher GitHub rate limits during analysis
GITHUB_TOKEN=
NEXT_PUBLIC_API_URL=http://localhost:8000

# Deep Scan (LLM architect review) — set ONE of these to enable.
# Direct Anthropic is preferred when both are set; OpenRouter routes to the
# same Claude models (~5% platform fee).
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
# DEEP_SCAN_DAILY_PER_IP=3
# DEEP_SCAN_MONTHLY_CAP_USD=100
